# Example credentials file for Chatbot PaperCare
# Copy this file to credentials.yml and configure as needed

# REST channel (required for webhook integration)
rest:
  # No additional configuration needed for basic setup

# Optional: Slack integration
# slack:
#   slack_token: "xoxb-your-bot-token"
#   slack_channel: "#general"
#   slack_signing_secret: "your-signing-secret"

# Optional: Facebook Messenger
# facebook:
#   verify: "your-verify-token"
#   secret: "your-app-secret"
#   page-access-token: "your-page-access-token"

# Optional: Telegram
# telegram:
#   access_token: "your-bot-token"
#   verify: "your-webhook-verify-token"
#   webhook_url: "https://yourdomain.com/webhooks/telegram/webhook"

# Optional: Voice connectors
# twilio:
#   account_sid: "your-account-sid"
#   auth_token: "your-auth-token"
#   twilio_number: "+1234567890"

# Socket.io for real-time communication (if needed)
# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: true

# Development/Testing
# cmdline:  # No additional configuration needed

# Note: This file should never be committed to version control
# Add credentials.yml to your .gitignore file